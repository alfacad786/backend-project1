<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>knc trust bharuch List</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <%- include("includes/userporternav copy.ejs") %>
          <form method="post" action="/trust/admin/back/<%=don.userName%>/">
        <button id="backbtn" type="submit">BACK</button>
      </form> 
    

    <div class="homain">
      <h1>Total donetion list</h1> 
 

      <div class="ta">
        <table>
          <thead>
            <tr>
              <th>userId</th>             
              <th>userName</th>
              <th>name</th>
              <th>payment</th>
              <th>date</th>
              <th>_id</th>
              <th>button</th>
            
            </tr>
          </thead>
          <% for(donor of fund) { %>
          <tbody>
            <tr>
              <td><%=donor.userId%></td>
              <td><%=donor.userName%></td>
              <td><%=donor.name%></td>
              <td><%=donor.payment%></td>
              <td><%=donor.date%></td>
              <td><%=donor._id%></td>
              <td><form method="post" action="/trust/admin/<%=donor._id%>?_method=DELETE">
                <button class="btn">delete</button>
              </form></td>              
            </tr>
            
          </tbody>
          <%}%>
        </table>
        <div id="btn">
           
        
         
        </div>
      </div>
    </div>
  </body>
</html>
