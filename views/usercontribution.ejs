<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>knc trust bharuch List</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <%- include("includes/newdonor.ejs") %>
    <div class="homain">
      <h1>My Contribution</h1>
      <h5>usercontribution.ejs</h5>

      <div class="ta">
        <table>
          <thead>
            <tr>
              <th>_id</th>
              <th>userName</th>
              <th>name</th>
              <th>payment</th>
              <th>date</th>
            </tr>
          </thead>
          <% for(donor of don) { %>
          <tbody>
            <tr>
              <td><%=donor._id%></td>
              <td><%=donor.userName%></td>
             
              <td><%=donor.name%></td>
              <td><%=donor.payment%></td>
              <td><%=donor.date%></td>              
            </tr>
          </tbody>
          <%}%>
        </table>
        <form method="post" action="/trust/user/back/<%=don[0].userId%>/">
          <button class="=btn" type="submit">BACK</button>
        </form>

        <div id="btn"></div>
      </div>
    </div>
  </body>
</html>
